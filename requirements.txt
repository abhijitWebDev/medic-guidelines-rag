# Runtime dependencies for the deployed API + web UI.
# Mirrors [project.dependencies] in pyproject.toml -- NOT the ingest (pymupdf)
# or legacy Streamlit extras, which together are ~290 MB and are not on the
# query path. Vercel's unzipped bundle limit is 250 MB.
fastapi>=0.141.1
httpx>=0.28.1
openai>=3.5.0
pydantic>=2.13.4
pydantic-settings>=2.15.0
python-dotenv>=1.2.3
redis>=5.0.0
pyyaml>=6.0.3
rank-bm25>=0.2.2
rich>=15.0.0
tiktoken>=0.14.0
numpy>=2.0
